langcode: en
status: true
dependencies: {  }
third_party_settings:
  contextual:
    mode: '0'
id: accelerator_media_title
label: 'Accelerator: Media title'
type: twig
description: 'Custom formatter for rendering media title in ''embedded'' display mode.'
field_types:
  - string
data: "{% for item in items %}\r\n  <h5 class=\"mb-0 py-1 px-2 bg-secondary text-white\"><small>{{ item.value }}</small></h5>\r\n{% endfor %}"
